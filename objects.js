var playlist={};

function updatePlaylist(playlist,artist,song){
  return assing.playlist({},{name:song})
}

function removeFromPlaylist(playlist,artist){
  delete playlist.artist
  return playlist 
}